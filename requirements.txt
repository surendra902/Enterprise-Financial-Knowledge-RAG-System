# FinRAG v3 — requirements.txt
# ─────────────────────────────────────────────────────────────────────────────
# Core pipeline
edgartools>=2.0
unstructured[html]>=0.12
qdrant-client>=1.8
sentence-transformers>=2.7      # CrossEncoder lives here
rank-bm25>=0.2.2
ollama>=0.2.0

# ★ v3 — Semantic cache (Redis Stack)
redis[hiredis]>=4.6.0           # includes redis.asyncio + hiredis C parser

# API
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.0

# Logging
loguru>=0.7

# ★ v3 — Optional evaluation metrics
rouge-score>=0.1.2              # ROUGE-1/2/L
bert-score>=0.3.13              # BERTScore (downloads DistilBERT ~268 MB on first use)
nltk>=3.8                       # BLEU + tokenisation
ragas>=0.1.10                   # Faithfulness / Answer Relevancy
datasets>=2.18                  # required by ragas

# HTTP client (for integration tests)
httpx>=0.27


# $env:REDIS_HOST="localhost"
# $env:REDIS_PORT="6380"